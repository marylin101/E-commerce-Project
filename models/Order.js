const orderFields = {
    _id: null,
    userId: null,
    addressId: null,
    items: Array,
    totalAmount: Number,
    status: String,
    paymentRef: String,
    createdAt: Date,
    updatedAt: Date,
};

module.exports = orderFields;